/**
 * Country-related types and interfaces
 */

export interface Country {
  name: string;
  dial_code: string;
  code: string;
}

export interface CountryInfo {
  name: string;
  dial_code: string;
}
